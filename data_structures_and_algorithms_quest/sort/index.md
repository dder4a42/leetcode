1. Top K：找到排序后的第 K 个元素
    - 排序，找到第 K 个元素
    - 大小为 K 的最小堆，令大于堆顶的元素入堆
    - 三路排序，仅处理 K 所在的子问题
3. 对链表进行原地插入排序：在有序牌堆中从左往右遍历即可。
    - dummy head 简化头节点讨论
    - 将待插入节点作为哨兵元素，即有序牌堆的末尾
2. 使数组元素相等的减少操作次数：跳台阶，转化为排序问题并贪心累积即可。